简单工厂
1、定义
  提供一个创建对象实例的功能，而无需关心其具体实现。被创建实例的类型可以是接口、抽象类，也可以是具体类
2、理解
   封装隔离的具体实现，目标是让客户端不知道封装的具体的实现
3、静态工厂
   静态工厂实际上就是不需要创建工厂（Factory）类，将工厂看成是一个工具类，工厂内部方法为静态方法
4、简单工厂通常不要创建太多的对象，一般一个工厂只创建一个组件或者模块
5、命名
     一般简单工厂以“模块名”+Factory，例如UserFactory.
     方法名称为get+接口名称，例如接口名称UserEbi,方法名称为getUserEbi。