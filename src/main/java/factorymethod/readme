1、工厂方法
定义： 一个用于创建对象的接口，让子类决定去实例化哪一个类，factory method使一个类的实例化延迟到子类

2、工厂方法主要是让父类在不知道具体实现的情况下，完成自身的功能调用，而具体的实现要延迟的子类来实现
3、父类通常是一个抽象类，里面包含一个抽象的factoryMethod方法，子类在实现这些抽象方法的时候，
   通常并不是真正的由子类来实现具体的功能，而是在子类的方法里面选择，选择具体的产品实现对象

4、父类可以是一个具体的实现类，在factoryMethod方法中，提供一个默认的实现对象，这样即使没有子类，
    也能够运行。通过子类覆盖父类的factoryMethod方法，提供更好的灵活性

5、工厂方法一般返回的是被创建对象的接口对象，方法可能需要参数，子类根据传递的参数进行选择

6、谁来使用工厂方法？实际上实有创建器本身内部的其他方法进行调用，工厂方法返回的对象，也可以提供给外部
   使用，但是一般不这么做。